apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - networkpolicy.yaml
# resourcequota.yaml and limitrange.yaml are intentionally NOT here — see
# ../manual/README.md. ArgoCD's auto-granted namespace role can't create
# those two kinds even with admin rights on everything else.
